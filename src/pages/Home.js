import React from 'react';
import { Section } from '../components';
import { Container } from '../globalStyles';

const Home = () => {
	return (
		<Container>
			<Section title='Ongoing Series' />
		</Container>
	);
};

export default Home;
